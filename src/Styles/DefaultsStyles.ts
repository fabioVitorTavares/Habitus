import { StyleSheet } from "react-native";

export const {centerItens, fullSize} = StyleSheet.create({
  
  centerItens: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  fullSize: {
    width: '100%',
    height: '100%'
  }
});